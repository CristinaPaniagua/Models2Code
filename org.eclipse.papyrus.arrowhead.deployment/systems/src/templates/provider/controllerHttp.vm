package eu.arrowhead.provider;

import org.springframework.http.MediaType;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.RestController;

#foreach( $dto in $dtos )
import eu.arrowhead.provider.dto.$dto;
#end

@RestController
@RequestMapping("/") 
public class ServiceControllerHttp {
	
	//=================================================================================================
	// members


	//=================================================================================================
	// methods
	
#foreach( $interface in $interfaces )
#set($operations=$interface.getOperations())
	#foreach( $operation in $operations )  
		#if($operation.getMethod() == "GET")
			#if($operation.isResponse()==true)
	@GetMapping(value="$operation.getPath()", produces = MediaType.APPLICATION_$interface.getEncoding()_VALUE)
    public $operation.capitalizeService()ResponseDTO $operation.getName() () {
		// Complete with your own code
		$operation.capitalizeService()ResponseDTO response= new $operation.capitalizeService()ResponseDTO();			
		return response;
    }
			#else 
	@GetMapping(value="$operation.getPath()", produces = MediaType.APPLICATION_$interface.getEncoding()_VALUE)
    public String $operation.getName() () {
		// Complete with your own code						
	 	String response= "";
        return response;
    }
			#end
		#else
				#if($operation.isRequest()==true)
    @PostMapping(value="$operation.getPath()", consumes = MediaType.APPLICATION_$interface.getEncoding()_VALUE, produces = MediaType.APPLICATION_$interface.getEncoding()_VALUE)
	@ResponseBody public String $operation.getName() (@RequestBody final $operation.capitalizeService()RequestDTO request) {
		// Complete with your own code               
       	String response= "";
    	return response;
	}
				#else
   	@PostMapping(value="$operation.getPath()", consumes = MediaType.APPLICATION_$interface.getEncoding()_VALUE, produces = MediaType.APPLICATION_$interface.getEncoding()_VALUE)
	@ResponseBody public String $operation.getName() (@RequestBody final String request) {
		// Complete with your own code               
    	String response= "";
    	return response;
	}
				#end
		#end    
	#end   
#end 
}